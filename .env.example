# Browser rendering (optional)
# CDP-compatible WebSocket endpoint for --render flag
# Works with Browserless, raw Chromium, Puppeteer clusters, etc.
# Remote endpoints must use wss:// (ws:// only allowed for localhost)
PSEOLINT_BROWSER_WS=
