{
  "name": "@pseolint/core",
  "version": "0.1.0",
  "description": "Programmatic SEO audit engine — 30 rules across 6 categories for SpamBrain risk detection",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ouranos-labs/pseolint.git",
    "directory": "packages/core"
  },
  "keywords": ["seo", "pseo", "programmatic-seo", "spambrain", "lint", "audit"],
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests tests/**/*.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "cheerio": "^1.2.0"
  },
  "peerDependencies": {
    "playwright-core": ">=1.40.0"
  },
  "peerDependenciesMeta": {
    "playwright-core": {
      "optional": true
    }
  }
}
