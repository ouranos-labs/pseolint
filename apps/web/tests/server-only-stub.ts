// Vitest no-op stub for Next.js's `server-only` runtime guard. The real
// package throws when bundled into a Client Component; tests bypass that
// check by aliasing this file in `vitest.config.ts`.
export {};
